# marketing-mfe

React marketing micro-frontend (login stub in Phase 1).

- **Now:** Vite + React, feature folders, json-server auth
- **Phase 7:** migrate to Next.js App Router (SSR, `/pricing`, `/docs`)
- **Phase 9:** mount into Angular shell at `/`

## Dev

```bash
# terminal 1 — mock API
npm run api

# terminal 2 — marketing login stub
npm run dev:marketing
```

Open http://localhost:4300 — login with `test@example.com` / `password123`.
